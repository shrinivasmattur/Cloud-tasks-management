version: '3.8'

services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: task-backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://2023isshrinivasmatturd_db_user:Veupt1nhcJDWqizs@cloudtaskcluster.bslc6nd.mongodb.net/taskmanager?retryWrites=true&w=majority
      - JWT_SECRET=supersecretkey12345
    networks:
      - task-network

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: task-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - task-network

networks:
  task-network:
    driver: bridge
